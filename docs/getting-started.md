# Getting Started

Proteus projects are packaged into `.prot` files that can be opened with Proteus Player.

## Create a Project

Use Proteus Author to arrange your source takes, configure playback behavior, and export the finished `.prot` file.

## Try a Demo

The downloads page includes sample `.prot` files and a demo project archive for testing the current tooling.
